# بوابة العدل السودانية - تعليمات التشغيل المحلي 🇸🇩

لحل مشكلة `'vite' is not recognized` وتشغيل الموقع في جهازك، اتبع الخطوات التالية بالترتيب:

### 1. تثبيت المكتبات (هام جداً)
افتح نافذة الأوامر (PowerShell) في مجلد المشروع واكتب:
```bash
npm install
```
*انتظر حتى ينتهي التحميل تماماً، سيقوم هذا الأمر بإنشاء مجلد `node_modules` الذي يحتوي على محرك التشغيل.*

### 2. تشغيل الموقع في وضع التطوير
بعد انتهاء التثبيت، اكتب الأمر التالي:
```bash
npm run dev
```

### 3. فتح الموقع
سيظهر لك رابط مثل `http://localhost:5173`. قم بالضغط عليه (أو Ctrl + Click) ليفتح الموقع في المتصفح.

---
**ملاحظة لمفتاح الـ API:**
تأكد من وجود ملف `.env` في المجلد الرئيسي يحتوي على:
`API_KEY=your_gemini_key_here`